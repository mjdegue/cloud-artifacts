# TODO: the Anthropic key secret — same pattern as Week 2 (container in Terraform,
# value via `aws secretsmanager put-secret-value`, recovery_window your call).
# Use a DIFFERENT name than week 2's (e.g. "memo-writer-v2/anthropic-key") so both
# artifacts can coexist if 02 is ever re-applied.
