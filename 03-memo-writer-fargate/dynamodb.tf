# TODO: the memos table — you wrote this in Week 2 from scratch; write it again
# from memory as a self-test (id hash key, on-demand, TTL on expires_at).
# Recreating it here keeps artifact 03 self-contained and destroyable on its own.
